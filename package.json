{
  "name": "recipeui",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "web:dev": "turbo run dev --filter=web",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanup_node_modules": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "encoding": "^0.1.13",
    "eslint-config-custom": "workspace:*",
    "turbo": "^1.10.12"
  }
}
